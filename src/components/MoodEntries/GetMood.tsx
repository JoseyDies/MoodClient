import { Component } from "react";

type propTypes = {
    token: string
}


export default class GetMood extends Component<propTypes, {}> {
    render () {
        return (
            <div></div>
        )
    }
}